<% if (!page.isCatalog) { %>
    <section class="px-6 max-w-prose mx-auto md:px-0">
        <!-- header -->
        <header class="overflow-hidden pt-6 pb-6 md:pt-12">
            <div class="pt-4 md:pt-6">
                <h1 id="article-title" class="heti--serif text-[2rem] font-bold leading-snug mb-4 md:mb-6 md:text-[2.6rem]">
                    <%= _p('books.title') %>
                </h1>
            </div>
        </header>
        <%- partial('_partial/bookshelf', { books: page.books }) %>
        <!--        <ul class="text-lg">-->
        <!--            <% page.books.forEach(book => { %>-->
        <!--                <li>-->
        <!--                    <a href="<%= url_for(`books/${book.path}`) %>"><%= book.title %></a>-->
        <!--                </li>-->
        <!--            <% }); %>-->
        <!--        </ul>-->
    </section>

<% } else { %>

    <section class="px-6 max-w-prose mx-auto md:px-0">
        <!-- header -->
        <header class="overflow-hidden pt-6 pb-6 md:pt-12">
            <div class="pt-4 md:pt-6">
                <h1 id="article-title" class="heti--serif text-[2rem] font-bold leading-snug mb-4 md:mb-6 md:text-[2.6rem]">
                    <%= page.book.title %>
                </h1>
            </div>
        </header>
        <ul class="w-full">
            <li class="text-2xl text-center p-5">CONTENTS</li>
            <%- partial('_partial/chapter', { book: page.book }) %>
        </ul>
    </section>

<% } %>
